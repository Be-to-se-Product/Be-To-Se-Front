import React from "react";

const HeaderCardLoja = ({children}) => {
  return (
    <div className="flex justify-between  items-center">
      {children}
    </div>
  );
};

export default HeaderCardLoja;
